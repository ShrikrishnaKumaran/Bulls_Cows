# Backend Environment Variables for Render.com Deployment
# Copy these to your Render dashboard Environment settings

# Server
NODE_ENV=production
PORT=5000

# Database - Your MongoDB Atlas connection string
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/bulls_cows_db?retryWrites=true&w=majority

# JWT Secrets (generate strong random strings for production!)
JWT_SECRET=generate-a-strong-random-string-here
JWT_REFRESH_SECRET=generate-another-strong-random-string-here

# Frontend URL for CORS (your Render frontend URL, no trailing slash)
CLIENT_URL=https://your-frontend-app.onrender.com
